# Password Check

**This is a very basic program using regular expression module (re) in Python to search for patterns in a string.**

**Checks the given str param if it is a valid password or not by searching some patterns in the string.**
